/** Stub — Pi embedded Google helpers gutted in RemoteClaw fork (Middleware Boundary Principle). */
export const resolveGoogleTranscriptPolicy = (..._args: unknown[]) => undefined as unknown;
export const isGoogleModelApi = (..._args: unknown[]) => false;

# RemoteClaw Slack

Official RemoteClaw channel plugin for Slack channels, DMs, commands, and app events.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/slack
```

Configure the Slack app credentials and allowed workspaces/channels in RemoteClaw. The plugin lets agents receive Slack events and reply through the configured Slack app.

# RemoteClaw Google Chat

Official RemoteClaw channel plugin for Google Chat spaces and direct messages.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/googlechat
```

Configure the Google Chat app credentials and allowed spaces in RemoteClaw. The plugin lets agents receive Google Chat events and reply through the configured app.

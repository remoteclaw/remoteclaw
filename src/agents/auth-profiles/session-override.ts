// Gutted in RemoteClaw fork (Middleware Boundary Principle)
export type SessionOverride = Record<string, unknown>;
export const resolveSessionOverride = (..._args: unknown[]) => undefined as unknown;

export const clearSessionAuthProfileOverride = (..._args: unknown[]) => undefined as unknown;

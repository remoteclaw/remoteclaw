import { describe, it } from "vitest";

// Gutted in RemoteClaw fork — buildRunBaseParams and buildRunContexts no longer exist
describe.skip("agent-runner-utils (gutted)", () => {
  it.skip("builds run base params with run metadata", () => {});
  it.skip("builds run contexts from run and session context", () => {});
  it.skip("prefers OriginatingChannel over Provider for messageProvider", () => {});
});

// Gutted in RemoteClaw fork (Middleware Boundary Principle).
// Retained as a no-op because gateway/server.impl still calls it.
export const getActiveEmbeddedRunCount = (..._args: unknown[]): number => 0;

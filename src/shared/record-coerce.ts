// The package copy is equally import-free / browser-safe (zero imports), so
// browser bundles still pull in no Node-only side effects via this re-export.
export * from "@remoteclaw/normalization-core/record-coerce";

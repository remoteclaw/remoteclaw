// Gutted in RemoteClaw fork (Middleware Boundary Principle)
// Original imports exec-safe-bin-policy and exec-safe-bin-trust which are gutted

export function normalizeExecSafeBinProfilesInConfig(_cfg: unknown): void {
  // No-op in RemoteClaw fork
}

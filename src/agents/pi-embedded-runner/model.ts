/* eslint-disable @typescript-eslint/no-explicit-any */
// Gutted in RemoteClaw fork (Middleware Boundary Principle).
// Retained as a permissive no-op because tts-core still references resolveModel.
export const resolveModel = (..._args: unknown[]): any => undefined;

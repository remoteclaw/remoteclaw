# RemoteClaw Microsoft Teams

Official RemoteClaw channel plugin for Microsoft Teams bot conversations.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/msteams
```

Configure the Teams bot credentials and trusted service URLs in RemoteClaw, then connect the bot to the teams or chats where agents should operate.

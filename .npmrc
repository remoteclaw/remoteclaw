# pnpm build-script allowlist lives in pnpm-workspace.yaml -> onlyBuiltDependencies.
# Dependency version pins live there too, under `overrides` (#3144). pnpm 10 warns
# that the `pnpm` field in package.json is no longer read, so nothing pnpm-related
# belongs there.

/* eslint-disable @typescript-eslint/no-explicit-any */
// Gutted in RemoteClaw fork (Middleware Boundary Principle).
// Retained as a permissive type alias because reply-payloads.ts depends on it.
export type MessagingToolSend = any;

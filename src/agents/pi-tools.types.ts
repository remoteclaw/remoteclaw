/** Stub — Pi tool types gutted in RemoteClaw fork (Middleware Boundary Principle). */
export type ToolPolicyConfig = Record<string, unknown>;
// eslint-disable-next-line @typescript-eslint/no-explicit-any -- stub type, consumed by production code
export type AnyAgentTool = any;

# RemoteClaw Matrix

Official RemoteClaw channel plugin for Matrix rooms and direct messages.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/matrix
```

Configure the Matrix homeserver and bot credentials in RemoteClaw. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.

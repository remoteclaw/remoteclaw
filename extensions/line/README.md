# RemoteClaw LINE

Official RemoteClaw channel plugin for LINE Bot API chats.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/line
```

Configure LINE channel credentials in RemoteClaw, then connect the bot to the chats where agents should receive and send messages.

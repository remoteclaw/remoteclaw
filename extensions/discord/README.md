# RemoteClaw Discord

Official RemoteClaw channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from RemoteClaw:

```bash
remoteclaw plugin add @remoteclaw/discord
```

Configure a Discord bot token and the channels or servers RemoteClaw should handle. The plugin lets RemoteClaw agents receive Discord messages and respond through the configured Discord app.
